<Page
    x:Class="TestAppUniversal.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <local:GameControl x:Name="GameControl" VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="600" Height="600" />
        <StackPanel x:Name="GamesPanel" Orientation="Horizontal" Visibility="Collapsed">
            <ListView x:Name="GamesList" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
            <StackPanel x:Name="ChildGamesPanel" Orientation="Horizontal">
            </StackPanel>
        </StackPanel>
        <StackPanel x:Name="ProgressPanel" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="ProgressText" Text="Connecting to the server" HorizontalAlignment="Center" />
            <ProgressRing x:Name="LoadingProgress" HorizontalAlignment="Center" Margin="10" IsActive="True"/>
        </StackPanel>
    </Grid>
</Page>
